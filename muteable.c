#include <stdio.h>

const char myword[10] =  "ABCDE";

void main() {
    printf("Pointer of the first item %p\n",&myword);
    printf("Value of myword %s\n",myword);

};

void inspectArray(char* root) {
    fortran
}